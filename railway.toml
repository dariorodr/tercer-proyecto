[build]
buildCommand = "pip install -r requirements.txt && python proyecto/manage.py collectstatic --noinput"

[deploy]
startCommand = "gunicorn --chdir proyecto proyecto.wsgi:application --bind 0.0.0.0:8080 --timeout 120"